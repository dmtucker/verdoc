[build-system]
requires = ['setuptools ~= 41.2.0', 'setuptools-scm ~= 3.3.3']
build-backend = 'setuptools.build_meta'

[tool.pylint.messages_control]
disable = "C0330, C0326"

[tool.pylint.format]
max-line-length = "88"
